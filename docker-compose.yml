version: "3.9"
services:
  nodejs-one:
    build: ./nodejs-one
    volumes:
      - ./nodejs-one:/nodejs-one
    environment:
      VERSION: ${VERSION}

  nodejs-two:
    build: ./nodejs-two
    volumes:
      - ./nodejs-two:/nodejs-two
    environment:
      VERSION: ${VERSION}